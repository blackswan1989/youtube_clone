extends layouts/main

block content 
    .form-container
        h1.upload__title <i class="fas fa-upload"></i> Video Upload
        h5 동영상을 제출하면 YourTube 서비스 약관 및 커뮤니티 가이드에 동의하게 됩니다.
        h5 타인의 저작권 또는 개인정보 보호 권한을 침해해서는 안 됩니다.
        form.upload(action=`/videos${routes.upload}`, method="post", enctype="multipart/form-data")
            input.title(type="text", placeholder="Title", name="title", required=true)
            textarea.description(name="description", placeholder="Description", required=true)
            div.upload__column
                div.fileUpload
                    label(for="file",) <i class="fas fa-folder-open"></i> Open File
                    input.upload__input(type="file", id="file", name="videoFile", onchange="document.getElementById('file_route').value=this.value", required=true, accept="video/*")
                input#file_route(type="text", title="File Route", readonly="readonly")
                input.uploadVideo(type="submit", value="Upload Video")
        .record-container#jsrecorderContainer
            |🍿 Video Recorder Preview 🍿
            video#jsRecordVideoPreview 
            button#jsRecordBtn <i class="fas fa-play-circle"></i> Start Recording



//- http://localhost:4000/videos/upload